# Template placeholder secrets — replace with real values in your project.
# This file is gitignored in generated projects by default; these dummy
# values only exist so a freshly rendered project builds out of the box.
signing_key_alias=changeme
signing_keystore_password=changeme
