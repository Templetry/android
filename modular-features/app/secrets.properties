# Template placeholder secrets — replace with real values in your project.
# This file is gitignored in generated projects by default; these dummy
# values only exist so a freshly rendered project builds out of the box.
signing_key_alias=changeme
signing_keystore_password=changeme

# Per-environment API base URL (ADR-0018), read into BuildConfig.API_BASE_URL
# by each flavor in CoreFlavor.kt. Not secrets themselves — kept here because
# this is the one file already gitignored per developer machine.
API_BASE_URL_DEVELOPMENT=http://10.0.2.2:8080
API_BASE_URL_STAGING=https://staging.example.com
API_BASE_URL_PRODUCTION=https://api.example.com
